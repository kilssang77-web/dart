---
runId: 2026-06-05_22-05-27
task: ml-retraining
stage_from: mvp
stage_to: mvp
completed_at: 2026-06-05T22:05:27+09:00
synced: true
---

## 변경 요약

### Step 1: 스타트업 복구 버그 수정 — recommender

- `services/recommender/main.py`: `_recover_missed_events()` 다중 버그 수정
  - `ml_client.py` SQL `macd_hist` 컬럼 없음 → `(macd - macd_signal) AS macd_hist` 계산 컬럼으로 대체
  - `entry_recommender._risk()`: `event.get("volume_ratio")` 키가 존재하지만 값이 None인 경우 `float()` 실패 → `or 1.0` 패턴으로 수정 (change_rate, _risk_factors도 동일)
  - `_save()`: `rec["created_at"]`이 문자열(`isoformat()`)이었으나 asyncpg는 `datetime` 객체 요구 → `datetime.now(timezone.utc)` 로 변경
  - 복구 이벤트 dict의 `Decimal` 타입 → `orjson.dumps` 직렬화 오류 수정 (`_f()` 헬퍼로 명시적 `float()`/`int()` 변환)
- **결과**: Recovery: completed 134/134 events

### Step 2: LightGBM 모델 재학습 — train_model.py 다중 버그 수정

`scripts/train_model.py` 및 `services/ml/train_model.py` 동시 수정:

- **SQL 컬럼 오류**: `sd.short_sell_vol` → `db.short_sell_vol` (daily_bars 소속 컬럼)
- **supply_demand 공백 대응**: `COALESCE(sd.foreign_net, db.foreign_net_buy)` 등으로 daily_bars fallback 추가
- **asyncpg date 타입**: `start`/`end` 문자열 → `date_type.fromisoformat()` 변환 후 전달
- **`UnboundLocalError: pd`**: 함수 내부 `import pandas as pd` 중복 선언이 모듈 레벨 `pd`를 지역변수로 섀도잉 → 중복 import 제거
- **피처 수 불일치(37 vs 43)**: `avail = [c for c in FEATURE_COLUMNS if c in df.columns]` 패턴 삭제 → 누락 피처를 0으로 채워 항상 43개로 학습
  ```python
  for col in FEATURE_COLUMNS:
      if col not in df.columns:
          df[col] = 0.0
  X = df[FEATURE_COLUMNS].fillna(0)
  ```
- **NameError: avail** (feature importance 출력): `index=avail` → `index=FEATURE_COLUMNS`
- **FutureWarning 제거**: `technical.py`의 `ma.pct_change(3)` → `ma.pct_change(3, fill_method=None)`

`services/ml/main.py`의 `_run_retrain()` 동일하게 수정 (주간 자동 재학습 경로도 정합성 확보).

### Step 3: LightGBM 43피처 모델 학습 완료

- 학습 범위: 2025-09-18 ~ 2026-06-05
- 학습 데이터: 278,088행, 2,783종목
- Entry 모델 AUC: **0.7546** (binary_logloss 수렴)
- Risk 모델 AUC: **0.7800**
- 양성률: 18.2% (5일 내 +5% 이상)
- Class 1 precision: 64% / 전체 accuracy: 83%
- 주요 피처 (importance 상위): `macd_hist`, `atr_ratio`, `rsi14`, `ma60_ratio`, `lower_wick`
- 모델 저장: `/models/lgbm/entry_model.lgb`, `/models/lgbm/risk_model.lgb` (model_cache 볼륨)

### Step 4: ML 추론 임계값 조정 및 적용

- ML 모델 확률 스케일이 rule-based 폴백(0.575 고정)과 달리 0.07~0.43 범위로 분포
  → BUY 임계값 `REC_MIN_PROB` 0.55 → **0.35** 로 조정
- `.env`: `REC_MIN_PROB=0.35`, `REC_MAX_RISK=0.60`, `REC_MIN_RISK_REWARD=2.0` 추가
- `services/api/routers/recommendations.py`: `list_recommendations` 및 `get_buy_signals`의 `min_prob` 기본값 0.55/0.60 → **0.35** 로 통일
- recommender 컨테이너 재생성(`docker compose up --force-recreate`)으로 환경변수 반영

### Step 5: 검증

- `[MLClient] entry_model loaded` / `risk_model loaded` 로그 확인
- 추론 결과: LG(003550) `VI_TRIGGERED` `ml_prob=0.4266`, BUY 판정
- 기존 rule-based 고정값(0.59) 탈피, 종목별 실측 확률 생성 확인
- `GET /api/v1/recommendations?action=BUY` → BUY 2건 정상 반환

## 주요 변경 파일

| 파일 | 변경 내용 |
|------|----------|
| `services/recommender/ml_client.py` | macd_hist SQL 수정 |
| `services/recommender/entry_recommender.py` | None-safe float 변환 |
| `services/recommender/main.py` | created_at datetime 타입, Decimal 직렬화 |
| `services/ml/train_model.py` | SQL/date/피처수/import 버그 수정 |
| `scripts/train_model.py` | 동일 |
| `services/ml/main.py` | _run_retrain() SQL/date/피처수 수정 |
| `services/ml/features/technical.py` | pct_change FutureWarning 제거 |
| `services/api/routers/recommendations.py` | min_prob 기본값 0.35 |
| `.env` | REC_MIN_PROB=0.35 추가 |

## 알려진 한계 및 다음 단계

1. **확률 미보정(uncalibrated)**: 18.2% 양성률 대비 모델 출력 범위가 낮음(0.07~0.43). 향후 Platt Scaling 또는 IsotonicRegression 보정 검토
2. **supply_demand 데이터 공백**: `supply_demand` 테이블이 비어있어 외국인·기관 수급 피처 전부 0. collector에서 KIS 수급 API 연동 시 모델 재학습 필요
3. **KOSPI 지수 데이터 공백**: `daily_bars`에 code='0001' 없음 → 시장 상대강도 피처 0. KOSPI 수집 로직 추가 후 재학습 권장
4. **유사사례 부족**: `sim_count=0` 지속 — `pattern_vector` 적재 후 pgvector 유사도 검색 활성화 필요
