---
runId: 2026-06-05_22-51-38
task: p1-core-features
stage_from: mvp
stage_to: mvp
completed_at: 2026-06-06T00:00:00+09:00
synced: true
---

## 변경 요약

### Step 1: FEATURE_COLUMNS 단일화 (ml_client.py ↔ lgbm_predictor.py)

- `services/recommender/ml_client.py` 수정:
  - `_FEATURE_COLUMNS_DEFAULT` (43개): 모델 미로드 시 fallback 전용으로 변경
  - `_try_load_models()`: `_entry_model.feature_name()`으로 동적 로드 → 모델 재학습 후 자동 동기화
  - `FEATURE_COLUMNS: list[str]` = 로드 성공 시 model 기준, fallback 시 DEFAULT
- **결과**: lgbm_predictor.py / ml_client.py 간 43개 컬럼 중복 제거 → 모델 업데이트 시 자동 동기화

### Step 2: ML 추론 API 분리 (FastAPI 서비스)

- `services/ml/main.py` 전면 재작성:
  - 기존 asyncio 단일 루프 → FastAPI + uvicorn lifespan 구조로 전환
  - 3개 엔드포인트: `GET /health`, `POST /predict`, `POST /reload`
  - `_result_update_loop`, `_weekly_retrain_loop` → asyncio.create_task로 백그라운드 실행
  - Weekly retrain: atomic 교체 (`tmp/` → 실제 경로) + 전역 predictor 핫스왑
  - `_run_retrain`: 파일 교체 루프에 `.pkl` 캘리브레이터 포함
- `services/ml/requirements.txt`: `fastapi==0.111.0`, `uvicorn[standard]==0.30.1`, `httpx==0.27.0` 추가
- `services/recommender/ml_client.py` 수정:
  - `_ML_SERVICE_URL = os.environ.get("ML_SERVICE_URL", "")` 추가
  - `get_ml_result()`: HTTP 추론 우선 → local LightGBM fallback → 규칙 기반 fallback 순서
- `services/recommender/requirements.txt`: `httpx==0.27.0` 추가
- `docker-compose.yml`: ml 서비스에 `ports: ["8001:8001"]`, recommender에 `ML_SERVICE_URL: "http://ml:8001"` 추가
- `.env.example`: `ML_SERVICE_URL`, `ML_API_PORT` 항목 추가
- **결과**: `http://localhost:8001/health` → `{"status": "ok", "model_loaded": true}` 확인

### Step 3: 확률 보정 (Isotonic Calibration)

- `services/ml/models/trainer.py` 수정:
  - `train_entry()`, `train_risk()`: validation set 예측값으로 `IsotonicRegression(out_of_bounds="clip")` 피팅
  - `entry_calibrator.pkl`, `risk_calibrator.pkl` → `{model_dir}/` 저장
  - Brier score 로깅 추가
- `services/ml/models/lgbm_predictor.py` 수정:
  - `_entry_cal`, `_risk_cal` 속성 추가
  - `load()`: 캘리브레이터 `.pkl` 파일 optional 로드 (joblib)
  - `_infer(model, calibrator, X, default)`: raw → calibrator → clip 순서로 처리
- `services/ml/train_model.py`: Brier score 출력 추가
- `services/ml/main.py`: `_run_retrain` weekly loop에 Brier score 로깅 추가
- **결과**: 재학습 시 `entry_calibrator.pkl`, `risk_calibrator.pkl` 자동 생성 → 다음 `/reload` 시 로드

### Step 4: 백테스트 검증

- `scripts/backtest_run.py` 전면 개선:
  - `--mode events` (기존): feature_events 테이블 기반
  - `--mode replay` (신규): daily_bars에서 VOLUME_SURGE / BREAKOUT_52W 규칙 직접 재적용
  - date 타입 처리: asyncpg `date_type.fromisoformat()` 변환 추가
- `services/ml/backtest/engine.py` 최적화:
  - `bars.groupby("code")` 사전 인덱싱 → O(n²) → O(n) 조회
  - `_get_future_bars()` 제거, 인라인 처리로 교체

**백테스트 결과 (2025-09-18 ~ 2026-04-30, stop=5%, target=10%, hold=10일)**:

| 신호 | 거래수 | 승률 | 평균수익률 | Profit Factor | Sharpe |
|------|--------|------|------------|---------------|--------|
| VOLUME_SURGE (x5.0) | 5,394 | 35.7% | 0.17% | 1.06 | 0.13 |
| BREAKOUT_52W | 14,900 | 40.3% | 0.64% | 1.23 | 0.47 |

**분석**:
- Raw 신호만으로도 2:1 R:R 기준 손익분기 돌파 (33.3% 필요)
- BREAKOUT_52W가 VOLUME_SURGE 대비 quality 우위 (승률 +4.6%, PF +0.17)
- ML 필터(REC_MIN_PROB=0.55) 적용 시 하위 신호 제거 → 승률/PF 추가 개선 예상
- 현 파라미터 유지 권고 (stop 5%, target 10%, hold 10일)

---

## 주요 변경 파일

| 파일 | 변경 내용 |
|------|----------|
| `services/ml/main.py` | FastAPI 앱 전환, /predict /reload /health 엔드포인트, weekly retrain calibrator 포함 |
| `services/ml/models/trainer.py` | IsotonicRegression 캘리브레이터 학습/저장 |
| `services/ml/models/lgbm_predictor.py` | 캘리브레이터 로드/적용 |
| `services/ml/train_model.py` | Brier score 로깅 추가 |
| `services/ml/requirements.txt` | fastapi, uvicorn, httpx 추가 |
| `services/ml/backtest/engine.py` | bars pre-indexing 최적화, iterrows 버그 수정 |
| `services/recommender/ml_client.py` | HTTP inference 우선, FEATURE_COLUMNS 동적 로드 |
| `services/recommender/requirements.txt` | httpx 추가 |
| `scripts/backtest_run.py` | replay 모드 추가, date 타입 수정 |
| `docker-compose.yml` | ml 포트 바인딩, recommender ML_SERVICE_URL |
| `.env.example` | ML 서비스 URL 항목 추가 |

## 검증 결과

- `GET /health` → `{"status": "ok", "model_loaded": true}` ✓
- `POST /predict` (sample features) → `success_prob=0.0785, model_used=true` ✓
- Recommender 재시작 후 Recovery 0건 ✓
- 백테스트 보고서 → `output/backtest_report.json` 저장 완료
- 모델 재학습(entry AUC≈0.753) + 캘리브레이터 생성 → `/reload` 완료 예정

## 알려진 한계 및 다음 단계

1. **캘리브레이터 배포**: 현재 재학습 중. 완료 후 `POST /reload` 호출로 핫스왑.
2. **supply_demand 실데이터**: 2026-06-06 장 마감 후 EOD 루프 최초 실행 → KOSPI 상대강도 피처 활성화.
3. **백테스트 한계**: 8개월 데이터(단 하나의 시장 사이클). 1년 이상 축적 후 재분석 권장.
4. **ML 필터 효과 검증**: feature_events 충분 축적(3개월+) 후 signal vs. recommendation 비교 분석 필요.
