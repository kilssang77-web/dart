---
runId: 2026-06-15_17-29-28
task: task-49-53
synced: false
created_at: 2026-06-15T17:29:28+09:00
commits: [282b2dd, 7e631c3, 8ea3f53, cd410ab]
---

# Task #49~#53 통합 개선 — 릴리스 노트

## Task #49: 패턴벡터 v3 — sec4 제로패딩 완전 제거

**커밋:** 282b2dd

### 변경 내용
- `services/recommender/pattern_vector.py` 전면 재설계
- 기존 sec4: 71개 유효값 + 124개 제로패딩 = 196차원 (62.2% 낭비)
- 신규 sec4: 7개 × 20일 시계열(140) + 49개 스칼라 + 7개 예약 = 196차원 (3.6% 패딩)

| sec4 섹션 | 차원 | 내용 |
|-----------|------|------|
| [0:20]    | 20   | RSI14 시계열 (20일) |
| [20:40]   | 20   | MACD hist 시계열 |
| [40:60]   | 20   | BB%B 시계열 |
| [60:80]   | 20   | ATR ratio 시계열 |
| [80:100]  | 20   | 거래량 비율 시계열 |
| [100:120] | 20   | 외국인 순매수 시계열 |
| [120:140] | 20   | 기관 순매수 시계열 |
| [140:196] | 56   | 49개 스칼라 + 7 예약 |

- `update_pattern_vector()` SQL에 `open`, `amount` 컬럼 추가
- **백그라운드 백필 실행 중** (1.5M건 기존 벡터 재계산, ~2시간 소요)

---

## Task #50: KOSPI 방향성 피처 제거 → 상대강도 강화

**커밋:** 7e631c3

### 배경
기존 모델의 피처 중요도: kospi_return_5d(1627 splits), kospi_return_1d(1445 splits) → 모델이 KOSPI 방향 예측기로 과적합

### 변경 내용
- `services/ml/shared/feature_schema.py`: 65 → 67개 피처
  - 제거: `kospi_return_1d`, `kospi_return_5d`
  - 추가: `rel_strength_3d`, `rel_strength_10d`, `rel_strength_20d`, `kospi_vol_5d`
- `services/ml/walk_forward_train.py`: kr3d/kr10d/kr20d/kospi_vol_5d 계산 추가
- `services/recommender/ml_client.py`: 6-tuple KOSPI 수익률 처리, LIMIT 25
- `services/collector/redis_stats.py`: 3d/10d/20d/vol_5d Redis 캐시 갱신

**효과:** 재학습 시 종목 고유 모멘텀 (KOSPI 대비 상대강도) 기반으로 예측

---

## Task #53: 외부 학습 완료 → ML 핫리로드 자동화

**커밋:** 8ea3f53

### 변경 내용
- `services/ml/walk_forward_train.py`: 학습 완료 후 `redis.set("ml:model_updated", "1", ex=3600)`
- `services/ml/main.py`: `_model_watch_loop()` 코루틴 추가 (60초마다 폴링)
  - `ml:model_updated` 감지 → `predictor.load()` 핫스왑 + Prometheus AUC 갱신

**효과:** `make train` / `docker compose exec ml python walk_forward_train.py ...` 완료 후 ML 서비스 재시작 없이 자동 모델 교체

---

## Task #52: HNSW 인덱스 인프라 준비

**커밋:** cd410ab

### 변경 내용
- `docker-compose.yml` postgres 서비스에 `shm_size: '512m'` 추가, `maintenance_work_mem=512MB`
- `services/api/services/feature_service.py`: `SET LOCAL hnsw.ef_search = 100` 추가
- `services/recommender/ml_client.py`: 동일

### HNSW 현황
- 백그라운드에서 직렬 빌드 실행 중 (`max_parallel_maintenance_workers=0`)
- 1.5M × 256dim, 완료 시 `idx_fevent_pattern_hnsw` 인덱스 생성됨
- **완료 후 할 일**: `DROP INDEX idx_fevent_pattern; ALTER INDEX idx_fevent_pattern_hnsw RENAME TO idx_fevent_pattern;`
- docker-compose.yml의 shm_size 변경은 다음 postgres 재시작 시 적용

---

## 자체 검증 결과

| 항목 | 결과 |
|------|------|
| pattern_vector.py 단위 테스트 | PASS (256dim, NaN=0, Inf=0) |
| feature_schema.py 피처 수 | 67개 (kospi_return 2개 제거, 4개 추가) |
| 전체 Python 구문 검사 | 5개 파일 모두 OK |
| fstock-ml 헬스체크 | `{"status":"ok","model_loaded":true}` |
| fstock-api 헬스체크 | `{"status":"ok","components":{"postgres":"ok","redis":"ok"}}` |
| recommender 로그 | [BUY] 정상 추천 생성 확인 |

---

## 후속 작업 (자동 진행 중)

1. **벡터 백필** — 1.5M건 v3 재계산 (fstock-recommender 백그라운드, ~2시간)
2. **HNSW 인덱스** — 직렬 빌드 (fstock-postgres 백그라운드, ~30분~1시간)
3. **모델 재학습** — `make train` 실행 필요 (Task #50 새 피처 활성화)
