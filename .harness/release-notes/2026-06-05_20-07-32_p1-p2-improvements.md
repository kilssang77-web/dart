---
runId: 2026-06-05_20-07-32
task: p1-p2-improvements
stage_from: prototype
stage_to: mvp
completed_at: 2026-06-05T20:30:00+09:00
synced: true
---

## 변경 요약

### Step 1: P1-1 — 모닝스타 3봉 패턴 detector 연결

- `services/detector/main.py`: `_bar_buffer` 슬라이딩 버퍼 추가, `MORNING_STAR` 신호 발행
- `services/api/routers/features.py`: `EVENT_TYPES`에 `MORNING_STAR` 추가
- 기존 구현된 `detect_morning_star()` 로직을 실제 Kafka 파이프라인에 연결

### Step 2: P1-2 — ML 모델 주간 자동 재학습

- `services/ml/main.py` 전면 재작성:
  - `_result_update_loop()` / `_weekly_retrain_loop()` 병렬 실행 구조로 변경
  - 매주 일요일 02:00 KST 자동 재학습 (`_run_retrain()`)
  - 재학습 중 tmp 경로에 저장 후 atomic rename으로 모델 핫스왑
  - 재학습 실패 시 기존 모델 유지 (fallback 보장)

### Step 3: P1-3 — KOSPI 상대수익률 피처 수정

- `services/ml/features/technical.py`: `inject_market_features()` 메서드 추가
  - `kospi_return_1d`, `kospi_return_5d`, `rel_strength_5d`, `market_vol_ratio` 4개 피처 계산
  - 기존 항상 0이던 피처들을 실제 KOSPI 지수 데이터로 채움
- `scripts/train_model.py`: `load_market_data()` 함수 추가, KOSPI 데이터 주입 호출
- `services/ml/main.py` `_run_retrain()`: KOSPI 데이터 로드 및 주입 추가
- `infra/postgres/seed_data.sql`: KOSPI(0001), KOSDAQ(1001) 의사 종목 추가

### Step 4: P2-5 — signal-generated Kafka 토픽 연결

- `services/recommender/main.py`: BUY 액션 시 `signal-generated` 토픽에 추가 발행
- 간결한 신호 포맷 (code, action, entry_price, target_price, stop_loss, success_prob, risk_score)
- WAIT/SKIP은 발행 안 함 — 향후 알림 서비스가 이 토픽만 소비하면 됨

### Step 5: MVP 보강 — pytest 단위 테스트 신설

- `tests/__init__.py`, `tests/requirements-test.txt` 신설
- `tests/test_candlestick.py`: 15개 테스트 (장대양봉·망치형·모닝스타 완전 커버)
- `tests/test_entry_recommender.py`: 12개 테스트 (진입 판단·가격 계산·확률 혼합)
- **pytest 27/27 통과**

## profile.json 업데이트

- `tech`: spring-boot-3 → fastapi + python-3.12 + kafka + postgresql-16+timescaledb+pgvector
- `project_name`: kospi-feature-stock
- `architecture.style`: monolith → microservices

## CLAUDE.md 업데이트

- Spring Boot/React 템플릿 → Python/FastAPI/마이크로서비스 실제 규칙으로 교체
- 5개 CRITICAL 규칙, 코딩 표준, 아키텍처 원칙 실제 스택 기반으로 작성

## 다음 단계 권장사항

1. KOSPI 지수 수집 로직을 collector에 추가 (KIS 지수 API 호출)
2. `/a2m_sync_docs` 로 .harness/docs/ 반영
3. P3 항목 진행: 알림 서비스, Grafana 대시보드
